SET BDY(TARGET).

UCLIN.
ADD PROGRAM(YYZGREET)
    DISTLIB(YYZTPROG).

ADD PROGRAM(YYZSETCC)
    DISTLIB(YYZTPROG).

ADD SAMPLE($INFO)
    DISTLIB(YYZTSAMP).

ADD SAMPLE(DMOGREET)
    DISTLIB(YYZTSAMP).

ADD SAMPLE(DMOSETCC)
    DISTLIB(YYZTSAMP).
ENDUCL.

SET BDY(DLIB).

UCLIN.
ADD PROGRAM(YYZGREET)
    DISTLIB(YYZDPROG).

ADD PROGRAM(YYZSETCC)
    DISTLIB(YYZDPROG).

ADD SAMPLE($INFO)
    DISTLIB(YYZDSAMP).

ADD SAMPLE(DMOGREET)
    DISTLIB(YYZDSAMP).

ADD SAMPLE(DMOSETCC)
    DISTLIB(YYZDSAMP).
ENDUCL.
